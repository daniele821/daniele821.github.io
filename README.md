# add new subwesites

- create directory in the repo root, and immediatly one level down place a `index.html`, and a `favicon.png`

```
- github-website
  └── project
      ├── index.html 
      └── favicon.png
```
- manually change `index.html` in root directory to include the new subwebsite (this is to avoid using js!)
